[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @trustledger/shared build && pnpm --filter web build"

[deploy]
startCommand = "node apps/web/.next/standalone/server.js"
healthcheckPath = "/"
restartPolicyType = "on_failure"
