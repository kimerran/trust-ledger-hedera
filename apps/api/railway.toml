[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @trustledger/shared build && pnpm --filter api build"

[deploy]
startCommand = "pnpm --filter api db:migrate && node apps/api/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
